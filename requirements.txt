# DriftSentinel - Python Dependencies
# Install: pip install -r requirements.txt

# Kubernetes Python client — official client for interacting with the cluster API
kubernetes>=28.1.0,<31.0.0

# YAML parser — for parsing Helm output, config files, and policy files
PyYAML>=6.0,<7.0

# Deep dictionary comparison — for structured field-level drift detection
deepdiff>=6.0,<8.0

# Testing framework
pytest>=7.0,<9.0
